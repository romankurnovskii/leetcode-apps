{
  "scripts": {
    "prepare": "husky",
    "format": "npm run format:js && npm run format:json && npm run format:py",
    "format:js": "prettier --write \"**/*.{ts,tsx,js,jsx}\"",
    "format:json": "prettier --write 'data/**/*.json'",
    "format:py": "black scripts/ solutions/"
  },
  "devDependencies": {
    "husky": "^9.0.0",
    "prettier": "^3.0.0"
  }
}
